#include <stdio.h>
int main()
{
    int N, digit, largest = 0;
    scanf("%d", &N);
    while (N > 0)
    {
        digit = N % 10;
        if (digit > largest)
            largest = digit;
        N /= 10;
    }
    printf("%d", largest);
    return 0;
}
