#include <stdio.h>
int main()
{
    int M, N, i;
    scanf("%d %d", &M, &N);

    for(i = M; i <= N; i++)
    {
        if(i % 2 == 0 && i % 3 == 0)
            printf("%d ", i);
    }

    return 0;
}
