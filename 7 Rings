#include <stdio.h>
int main()
{
    int N, X, total;
    scanf("%d %d", &N, &X);
    total = N * X;

    if (total >= 10000 && total <= 99999)
        printf("Valid");
    else
        printf("Invalid");

    return 0;
}
