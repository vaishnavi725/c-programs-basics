#include <stdio.h>
int main() {
    double cp, sp;
    scanf("%lf %lf", &cp, &sp);
    double loss = cp - sp;
    double loss_percent = (loss / cp) * 100;
    printf("%.2f", loss_percent);
    return 0;
}
