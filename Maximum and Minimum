#include <stdio.h>

int main() {
    int n, i, j;
    scanf("%d", &n);
    int a[n], count[1001] = {0};
    for(i=0;i<n;i++){
        scanf("%d", &a[i]);
        count[a[i]]++;
    }
    int min = 100000, max = -1, flag = 0;
    for(i=0;i<n;i++){
        int v = a[i];
        if(v == count[v]){
            if(v < min) min = v;
            if(v > max) max = v;
            flag = 1;
        }
    }
    if(flag==0) printf("-1");
    else printf("%d %d", min, max);
    return 0;
}
