#include <stdio.h>

int main() {
    int X, Y;
    scanf("%d %d", &X, &Y);
    int total_expense = 30 * Y;
    if (X >= total_expense)
        printf("YES");
    else
        printf("NO");
    return 0;
}
