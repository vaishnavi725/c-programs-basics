#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    for (int i = 1; i <= N; i++) {
        int value = (i % 2 == 0) ? 1 : 0;
        for (int j = 1; j <= N; j++) {
            printf("%d ", value);
        }
        printf("\n");
    }

    return 0;
}
