#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    for (int i = N; i >= 1; i--) {
        char ch = 'A' + i - 1;
        for (int j = i; j >= 1; j--) {
            printf("%c ", ch);
        }
        printf("\n");
    }

    return 0;
}
