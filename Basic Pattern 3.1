#include <stdio.h>

int main() {
    int N;
    if (scanf("%d", &N) != 1) return 0;

    for (int i = N; i >= 1; i--) {
        for (int j = 0; j < i; j++) {
            printf("* ");
        }
        printf("\n");
    }
    return 0;
}
